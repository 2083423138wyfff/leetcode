# 错题本

记录刷题过程中值得复盘的题：核心思想、涉及知识点、犯的错误、正确思路。每题一节，持续追加。

---

## LeetCode 49. 字母异位词分组（Medium）

**核心思想：分组题 = 签名设计题。** 让每个单词算出一个"签名"，异位词签名相同，交给哈希表分组。

### 两种签名

1. **排序签名**：`''.join(sorted(word))`，每个单词 O(k log k)，简单好写
2. **计数签名**：每个单词 O(k)，理论最优，两种写法
   - 稀疏版（dict 计数）：`tuple(sorted(count.items()))`
   - 稠密版（长度 26 的数组计数）：`tuple(count)`——下标隐含字母、天然有序，不需要 items 也不需要 sorted

### 签名设计三问（检查清单）

1. **信息够吗？** 只用键丢次数：'aab'/'abb' 键签名都是 (0,1)，被误判同组；只用值丢字母：'ab'/'cd' 值签名都是 (1,1)，被误判同组
2. **与输入顺序无关吗？** dict 的 items 顺序 = 字母在单词中出现的顺序（eat → 4,0,19；tea → 19,4,0），必须 sorted 归一化；稠密数组下标天然有序，免疫此问题
3. **可哈希吗？** list / dict / set 不可哈希不能当键；tuple 可以

### 涉及的知识点

- 字典的键必须可哈希：str / int / tuple 可以，list / dict / set 不行
- **对字典直接操作，默认作用于键**：`for x in d`、`x in d`、`list(d)`、`tuple(d)`、`sorted(d)`、`len(d)` 全是键；要值或键值对必须显式 `.values()` / `.items()`
- `d[k] += x` 展开是 `d[k] = d[k] + x`，先读后写；k 不存在时读取阶段就 KeyError。正确计数写法：`d[k] = d.get(k, 0) + 1`
- `''.join(...)` 只能拼接字符串序列；`sorted()` 返回的是 list
- `ord(c) - ord('a')` 把字母映射到 0~25，计数签名的核心技巧
- ACM 输入解析要和数据类型匹配：单词用 `split()`，数字才用 `map(int, ...)`；`list(字符串)` 得到的是单字符列表
- `d.values()` 是方法调用，括号不能省

### 我的错误点（按出现顺序）

1. **输入解析类型不匹配**：单词输入用了 `map(int, ...)` → ValueError
2. **输入被拆成字符**：`list(readline().strip())` 把 6 个单词拆成 24 个字符——程序不报错但输出全错。**教训：不报错 ≠ 正确，输入解析完先打印确认**
3. **下标/值混淆**（复发×4，最高优先盯防）：`for i in range(len(strs))` 后对下标 `i` 做 `sorted(i)` → TypeError。169 题（`append(i)` 存下标）、438 题（`ord(i)` 对下标取字符，且第一次只修了出队那一半）同犯。**自检口诀：循环里先问"我要的是元素还是下标"，只要元素就用 `for x in xs`；一旦写了 `range(len(...))`，函数体里几乎处处该是 `xs[i]`**
4. **`hash.values` 漏括号** → TypeError: 方法对象不可迭代
5. **`+=` 计数写法错误**：`count[k] += count.get(k, 1)` → KeyError（`+=` 先读后写，`get` 的默认值只救得了赋值，救不了读取）
6. **签名信息不全**：`tuple(count)` 只剩键；`sorted(count.values())` 只剩次数
7. **join 滥用**：对 (int, int) 元组列表做 join → TypeError。key 不需要是字符串，只需要"可哈希 + 内容相同则相等"

### 正确代码

```python
import sys

def group_anagrams(strs):
    groups = {}
    for word in strs:
        key = ''.join(sorted(word))          # 排序签名
        groups.setdefault(key, []).append(word)
    return list(groups.values())

def group_anagrams_count(strs):
    groups = {}
    for word in strs:
        count = [0] * 26
        for c in word:
            count[ord(c) - ord('a')] += 1
        groups.setdefault(tuple(count), []).append(word)   # 稠密计数签名
    return list(groups.values())

strs = sys.stdin.readline().split()
print(group_anagrams(strs))
```

---

## LeetCode 239. 滑动窗口最大值（Hard）

**核心思想：单调双端队列。** 队列存下标，对应值从队首到队尾单调递减；队首即当前窗口最大值。"又大又新者通吃"——比新元素小的旧元素永无出头之日，直接踢掉。

### 涉及的知识点

- 单调队列的不变量：队首 → 队尾值递减 + 全部在窗口内
- **存下标不存值**：过期判断靠位置（`i - queue[0] >= k` 即队首已滑出窗口左界 `i-k+1`）
- 过期判断是 `>=` 不是 `>`：span 恰好等于 k 时队首已经出界
- 记录时机：`i >= k-1` 窗口才填满，此前不输出
- 每个元素最多入队、出队各一次 → O(n)
- `max()` 不能作用在空序列上；每步 `max(queue)` 重扫是 O(n·k)，单调队列的意义就是不重扫
- 窗口个数是 `n-k+1`（`range(len-k)` 会漏最后一个窗口）

### 我的错误点（按出现顺序）

1. **暴力版不自知**：每窗口 `max(nums[i:i+k])` = O(n·k)，k=n/2 时实测 n 翻倍耗时 ×4（平方增长指纹），还误以为自己是 O(n)。**教训：切片和 max 都是 O(k)，藏在循环里就是 O(n·k)**
2. **每轮循环新建 deque**：状态从不跨窗口保留，deque 纯摆设——删掉那三行输出不变。**教训：写数据结构前先问"它的状态要活多久"**
3. **`nums[0,k]` 元组索引**：TypeError。取切片是 `nums[0:k]`
4. **条件式入队闸门**（`if nums[i] > queue[-1] 才 append`）+ 每步 popleft → 队列只出不进，最终被踢空，`max()` 空序列崩溃。**教训：队列必须定义不变量，入队/出队规则要守恒**
5. **记录时机错**：`i - queue[0] == k` 几乎永远不触发（输出空数组）；正确是 `i >= k-1`
6. **过期判断 off-by-one**：`> k` 让过期元素多赖一轮（`[3,1,2] k=2`、`[5,1,2,3] k=3` 都死在这）；正确是 `>= k`

### 正确代码

```python
from collections import deque
import sys

def max_sliding_window(nums, k):
    dq = deque()                       # 存下标，对应值单调递减
    res = []
    for i, x in enumerate(nums):
        while dq and nums[dq[-1]] < x:
            dq.pop()                   # 又大又新者通吃
        dq.append(i)
        if i - dq[0] >= k:             # 队首滑出窗口
            dq.popleft()
        if i >= k - 1:                 # 窗口填满才记录
            res.append(nums[dq[0]])
    return res

nums = list(map(int, sys.stdin.readline().split()))
k = int(sys.stdin.readline().strip())
print(max_sliding_window(nums, k))
```

---

## LeetCode 76. 最小覆盖子串（Hard）

**核心思想：可变滑动窗口 + valid 计数器。** 右指针扩张找合法窗口，合法后左指针收缩榨最短；"涵盖"关系是 `window[c] >= need[c]`（不是全等），用"达标种数"计数器把合法性判断压到 O(1)。

### 涉及的知识点

- 可变窗口节奏：不合法就扩（右移），合法就缩（左移）——**收缩是 while 循环**，每次移动前先更新答案
- valid 计数器：`window[c] == need[c]` 那一刻 +1（恰好到达门槛才计）；移出后 `< need[c]` 才 -1
- 查 need 前先守卫：`key in need`（dict 的 in 默认查键），否则无关字符 KeyError
- 守卫的对象要和查询的对象一致（进来的字符守卫进来的，离开的字符守卫离开的）
- 找最小值的初始值要"谁都打得过"：`float('inf')`，不是 -1
- 提前返回的类型要和正常返回一致（都是 str）

### 我的错误点（按出现顺序）

1. **查 need 无守卫 → KeyError**：s 里 t 没有的字符（'D'、'O' 等）直接 `need[...]` 崩了；修复时又写成 `need[...] and ...`——**守卫本身就是那个会炸的查询**。正确姿势：`x in need`
2. **守卫与查询对象错位**：收缩处的守卫写的是 `s[right]`（刚进来的），查询的却是 `s[left]`（要走的）——崩溃只是从一处挪到另一处。**教训：守卫必须守在真正的查询前面**
3. **`leng = -1` 方向反了**：找最小值，初始值要让任何候选都能击败它
4. **`!=` 把"有富余"误判成"不达标"**：need 2 个 a、窗口 4 个 a、移出 1 个仍达标，`!=` 却误减 valid。跌破门槛的条件是 `<`
5. **提前返回 `-1` 与正常路径返回 str 类型不一致**（438 题同款）

### 正确代码

```python
import sys

def min_window(s, t):
    if not t or len(t) > len(s):
        return ""
    need = {}
    for c in t:
        need[c] = need.get(c, 0) + 1
    window = {}
    valid = 0
    left = 0
    leng = float('inf')
    res = ""
    for right in range(len(s)):
        c = s[right]
        window[c] = window.get(c, 0) + 1
        if c in need and window[c] == need[c]:   # 恰好达标才 +1
            valid += 1
        while valid == len(need):                # 合法就连续收缩
            if right - left + 1 < leng:
                leng = right - left + 1
                res = s[left:right + 1]
            d = s[left]
            window[d] -= 1
            if d in need and window[d] < need[d]:  # 跌破门槛才 -1
                valid -= 1
            left += 1
    return res

s = sys.stdin.readline().strip()
t = sys.stdin.readline().strip()
print(min_window(s, t))
```
