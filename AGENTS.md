# Agent Instructions

## 项目说明

本项目用于练习 LeetCode / 算法题，按算法类型分目录存放。

## 工作习惯

- 教学模式：采用 Socratic（苏格拉底式）教学方法，引导学生分 5 步完成题目。
- 代码原则：学生自己写出代码（或伪代码）之前，不直接给出完整代码。
- ACM 模式：当学生使用 `sys.stdin.readline()` / `print()` 时，按 ACM 竞赛模式处理。
- **临时文件清理：运行测试时生成的临时文件（如 `test_xxx.py`），用完后应立即删除，保持仓库整洁。**
